interface:
  display_name: "FA Server Operations"
  short_description: "通过 MCP 创建、查询和诊断 FA Server 文件任务"
  default_prompt: "Use $fa-server-operations to start and monitor an FA Server folder workflow."

dependencies:
  tools:
    - type: "mcp"
      value: "fa-server"
      description: "FA Server synchronization and super-resolution task tools"

policy:
  allow_implicit_invocation: true
